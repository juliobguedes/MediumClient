import React from 'react';
import { Input, Label } from '../common';

const App = () => (
  <div>
    <Label />
    <Input placeholder="Your first name, e.g. Júlio" />
  </div>
);

export default App;
